name: CI/CD (main)

on:
  push:
    branches: [main]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: 코드 체크아웃
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GIT_TOKEN }}

      - name: JDK 21 설정
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: gradle

      - name: Gradle 빌드
        run: |
          chmod +x gradlew
          ./gradlew bootJar -x test

      - name: Docker Buildx 설정
        uses: docker/setup-buildx-action@v3

      - name: Harbor 로그인
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.HARBOR_REGISTRY }}
          username: ${{ secrets.HARBOR_USERNAME }}
          password: ${{ secrets.HARBOR_PASSWORD }}

      - name: Docker 이미지 빌드 및 푸시
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ secrets.HARBOR_REGISTRY }}/skala26a-cloud/opentraum-user-service:${{ github.sha }}
            ${{ secrets.HARBOR_REGISTRY }}/skala26a-cloud/opentraum-user-service:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: k8s 매니페스트 이미지 태그 업데이트
        env:
          GIT_TOKEN: ${{ secrets.GIT_TOKEN }}
        run: |
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"
          git fetch origin main
          git checkout main
          sed -i "s|image: .*opentraum-user-service.*|image: ${{ secrets.HARBOR_REGISTRY }}/skala26a-cloud/opentraum-user-service:${{ github.sha }}|" k8s/deployment.yml
          git add k8s/deployment.yml
          git commit -m "chore: 이미지 태그 업데이트 [${{ github.sha }}] [skip ci]" || echo "변경사항 없음"
          git push origin main
